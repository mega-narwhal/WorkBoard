[pytest]
# Unit suite for WorkBoard's pure-logic + security invariants. Hermetic and
# fast — no network, no LLM, no live board/registry (see tests/conftest.py).
testpaths = tests
addopts = -q --strict-markers
